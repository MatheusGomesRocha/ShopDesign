import './Register.css';
import PathComponent from '../sideComponents/Path';

export default () => {
    return(
        <div className={"register"}>
            <PathComponent register={true} />
        </div>
    )
}